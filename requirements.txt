# Dependencias de las simulaciones (simulaciones/*.py)
# Instalar en un venv:  python -m venv venv && source venv/bin/activate && pip install -r requirements.txt
numpy
pandas
scipy
matplotlib
torch        # PyTorch; usa CUDA si hay GPU NVIDIA, si no corre en CPU (tier 1)
